<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "SocialNetwork" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- For all other devices -->
    <!-- Size should be 32 x 32 pixels -->
    <%= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>

    <%= javascript_include_tag "application" %>
  </head>
  <body>

    <main class="container-fluid">
      <div class="navbar navbar-fluid-top">
        <div class="navbar-inner">
          <div class="container-fluid">
            <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
              <span class="icon-bar"></span>
            </a>
            <a class="brand" href="#">Social Network</a>
            <div class="container-fluid nav-collapse" id='user_header'>
              <ul class="nav">
                <% if current_user %>
                  <li><%= link_to 'Edit profile', edit_user_path(current_user), class: 'pull-right' %></li>
                  <li><%= link_to 'Log out', logout_path, class: 'pull-right' %></li>
                <% else %>
                  <li><%= link_to 'Log in', login_path %></li>
                  <li><%= link_to 'Sign up', signup_path  %></li>
                <% end %>
              </ul>
            </div><!--/.nav-collapse -->
          </div>
        </div>
      </div>

      <div class="container-fluid">
        <div class="row-fluid">
          <div class="span3">
            <% if current_user %>
              <div class="well sidebar-nav">
                <ul class="nav nav-list">
                  <li><%= link_to 'My profile', current_user  %></li>
                  <li><%= link_to 'Messages', root_url  %></li>
                  <li><%= link_to "Link3", "/path3"  %></li>
                </ul>
              </div><!--/.well -->
            <% end %>
          </div><!--/span-->
          <div class="span9">
            <%= bootstrap_flash %>
            <%= yield %>
          </div>
        </div><!--/row-->

        <footer>
          <p>&copy; Company 2014</p>
        </footer>

      </div> <!-- /container -->
    </main>

  </body>
</html>
